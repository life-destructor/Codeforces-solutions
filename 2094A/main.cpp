#include <bits/stdc++.h>

using namespace std;

int main () {
    int t; cin >> t;
    while (t--) {
        string firstWord, secondWord, thirdWord;
        cin >> firstWord >> secondWord >> thirdWord;
        cout << firstWord[0] << secondWord[0] << thirdWord[0] << "\n";
    }
    return 0;
}