#include <iostream>

using namespace std;

int main () {
    int t, a, b, c;
    cin >> t;
    for (int i = 1; i <= t; i++) {
        cin >> a >> b >> c;
        // so basically i simulate the game and then see which one in the end is left out.
        //alright, test cases
        //if both numbers are even, and the c is even too, Katie just wins, and if c is odd in this case, Anna wins
        // 
    }
    return 0;
}