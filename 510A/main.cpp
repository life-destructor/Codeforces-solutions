#include <iostream>

using namespace std;

int main()
{
    int n, m;
    cin >> n >> m;
    const char = '#';

    // the tail is located at (1,1).
    // Then its body extends to (1,m). Then goes down 2 rows to (3,m). Then goes left to (3,1), and so on.
    // I'm gonna make an array for each line.
    int thisArray[]
    // oh, so basically the motion of going from top left to right, to bottom, to left again is done with every input. We just have to know how many times.
    for (int i = 1; i <= m; i++) {
        cout << ""
    }
    return 0;
}
