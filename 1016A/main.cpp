#include <bits/stdc++.h>

using namespace std;

int main () {
    int n, namesEachPage;
    int thisDay[n];
    vector<int> pageTurnEachDay;
    for (int i = 0; i < n; i++) {
        cin >> thisDay[i];
        if (thisDay[i] < namesEachPage) {
            pageTurnEachDay.push_back(0);
        } else {
            
        }
    }
    return 0;
}